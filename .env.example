# URL the extension fetches the bucket schedule + kill switch + banner from.
# Default targets the local dev server in ../better-caesar-server.
# Switch this to your hosted URL for production builds.
BC_BUCKET_SCHEDULE_URL=http://localhost:4173/bucket-schedule.json
