// Build-time constants injected by scripts/build.mjs via esbuild's `define`.
// Source of truth: BC_BUCKET_SCHEDULE_URL in `.env` (see `.env.example`).
declare const __BC_BUCKET_SCHEDULE_URL__: string;
